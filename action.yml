name: Canonical docs rules
author: '@evilnick'
description: 'Installs Vale rules from the Canonical documentation style guide.'
inputs:
  vale-config:  
    description: 'path/to/vale.ini to use'
    required: true
    default: './vale.ini'
outputs:
  random-number:
    description: "Random number"
    value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"
  steps:
    - name: check Vale config
      run: |
        if ! test -f ./vale.ini; then
        echo "No vale.ini found, using default"
        cp ${{ github.action_path }}/vale.ini .
        fi
        mkdir .styles
        cp -r ${{ github.action_path }}/styles .styles
        echo "Styles copied"
      shell: bash
    - id: random-number-generator
      run: echo "random-number=$(echo $RANDOM)" >> $GITHUB_OUTPUT
      shell: bash
    - run: echo "${{ github.action_path }}" >> $GITHUB_PATH
      shell: bash
